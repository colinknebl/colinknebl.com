import Layout from '../components/layout';
import SEO from '../components/seo';

export default ({ children }) => {
    return (
        <Layout>
            <SEO title='Uses' />
            {children}
        </Layout>
    );
};

# Uses

(coming soon)
